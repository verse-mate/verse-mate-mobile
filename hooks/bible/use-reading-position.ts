/**
 * useReadingPosition Hook
 *
 * Manages persistence of reading position (book + chapter).
 * Position is saved to API via POST /bible/book/chapter/save-last-read.
 *
 * @placeholder - Implementation will be added in Task Group 4
 */

import type { UseReadingPositionResult } from '@/types/bible';

export function useReadingPosition(): UseReadingPositionResult {
  // Placeholder implementation
  throw new Error('useReadingPosition hook not yet implemented. See Task Group 4.');
}
